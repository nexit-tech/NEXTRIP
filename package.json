{
  "name": "nextrip-flutter-web-server",
  "version": "1.0.0",
  "description": "Simple static server for Flutter Web deployment on Railway.",
  "scripts": {
    "postinstall": "bash build.sh", 
    "start": "npx serve -s build/web"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}