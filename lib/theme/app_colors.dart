import 'package:flutter/material.dart';

class AppColors {
  // Cores Principais (Monochromia)
  static const Color black = Color(0xFF000000);       // Fundo geral
  static const Color eerieBlack = Color(0xFF1B1B1B);  // Fundo dos Cards/Inputs
  static const Color chineseBlack = Color(0xFF141414);
  static const Color nightRider = Color(0xFF2E2E2E);  // Bordas ou divisores
  
  // Cores de Texto/Ícones
  static const Color white = Color(0xFFFFFFFF);           // Texto Principal / Botão Primary
  static const Color chineseWhite = Color(0xFFE1E1E1);    // Texto Secundário
  static const Color antiFlashWhite = Color(0xFFF3F3F3);
}